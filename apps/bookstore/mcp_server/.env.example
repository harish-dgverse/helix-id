# Bookstore MCP Server — Environment Variables
# Copy this file to .env and fill in the values for your local environment.
# The .env file is git-ignored and should never be committed.

# -------------------------------------------------------
# External service URLs
# -------------------------------------------------------

# Base URL of the Bookstore Next.js app
BOOKSTORE_API_BASE_URL=http://localhost:3000

# Base URL of the Helix-ID app backend (VP verification)
HELIX_ID_BACKEND_URL=http://localhost:4000

# -------------------------------------------------------
# MCP Server settings
# -------------------------------------------------------

# Host and port the MCP server listens on
MCP_SERVER_HOST=0.0.0.0
MCP_SERVER_PORT=8001
